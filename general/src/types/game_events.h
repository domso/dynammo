#pragma once

namespace types {
    enum game_events {
        clear,
        
        move_up,
        move_down,
        move_right,
        move_left,
        
        
        
        recv_tcp_link
    };
}
